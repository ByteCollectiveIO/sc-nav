# CLAUDE.md — repo map & navigation

Purpose of this file: let a fresh session find the right code **without reading
whole files**. Grep the banner conventions below; jump with `Read(offset/limit)`.
Keep this map current when you add a view, endpoint, or table.

## What this is
Star Citizen org tool — ten apps in one SPA: navigator, cargo + trade route
planners, Prospector (drop planner + belt survey, #38), event planner (+ fleet
rosters), group finder, danger board, resource manager, aUEC marketplace, org
intel. FastAPI backend, **single-file** SPA, plus a Windows watcher (Python
script) that reports the player's in-game position.

- Backend: `server/app.py` (HTTP/WS + routes), `server/nav_core.py` (pure nav/route
  logic, fully unit-tested), `server/db.py` (SQLite schema + queries).
- Frontend: `server/static/index.html` — ONE file: `<style>` + body + `<script>`.
- Watcher: `watcher/` (runs on the player's Windows box; reads Game.log).
- Version: `server/version.py` (SemVer; surfaced at `/api/health` + footer).
- Tests: `server/test_nav_core.py` + `server/test_app.py`. Deploy = push to
  `origin/main` (see `/deploy`).
- Docs: `docs/README.md` = index w/ per-doc status · `docs/product-overview.md`
  = consolidated app/service/data map · `docs/feature-backlog.md` = active work.

## DO NOT READ (token sinks / generated / binary)
- `server/.venv/**` — dependencies. Never read; never grep here.
- `poi/*.db`, `poi/*.db-wal/-shm` — SQLite binaries. Use `db.py` for schema.
- `poi/*.json` runtime caches (gitignored) AND the committed feeds
  (`poi.json`/`containers.json`/`container_tombstones.json`/`quantum_*`/`blueprints.json`/`locations.json`)
  — all token sinks. The schema is in code, not here.
- `.impeccable/`, `.github/skills/`, `.claude/skills/` — tooling, not app code.

## Navigation conventions (how to find things fast)
Every logical section is marked by a greppable banner. To locate code, grep the
banner — don't scroll.
- **index.html JS**:  `grep -n "// ----" server/static/index.html`
- **index.html CSS**: `grep -n "/* ----" server/static/index.html`
- **index.html views**: `grep -n 'id="[a-z-]*-view"' server/static/index.html`
- **app.py routes**:  `grep -nE "^@app\.(get|post|put|patch|delete)" server/app.py`
- **db tables**:      `grep -n "CREATE TABLE" server/db.py`

## index.html section index (~13722 lines; ranges drift — confirm by grep)
`<style>` lines 7–1997 · body 1999–3782 · `<script>` 3784–13720.

Body views (each a `#…-view` container, hash-routed):
launcher, main (navigator), settings, setup, intel, leaderboard, stats,
cargo-leaderboard, cargo-stats, route (cargo planner), events, goals, inventory,
blueprints (RM's third tab, #29), market, online (who's online, #19),
lfg (group finder / LFG board, #19), pirates (danger board / pirate warnings, #24),
halo (Prospector, #31/#38: masthead tabs `#/halo` DROP · `#/halo/field` FIELD · `#/halo/atlas` ATLAS — containers `halo-tab-drop/field/atlas`, `applyHaloTab`/`haloGoTab`, FIELD pin `renderHaloFly`, ATLAS `renderHaloAtlas*`), terms, privacy.

JS modules (by `// ----` banner): formatting · ore colors · ore value badges (#32) · resource forecast · state ·
freshness · shard · nearby · captures · destination · path/map · search ·
element finder · teammate presence · websocket · auth gate (+ PROFILE playstyle
chips, #30) · cargo planner + run mode · event planner · resource manager
(shared masthead `rmMast`; catalog picker / goals / inventory / my blueprint
library at `#/blueprints`, #29)
· marketplace · pirate danger board (#24) · halo finder / Prospector (#31, tabs #38) · view router · leaderboard · statistics · Org Intel · org settings
· org name + MOTD (`applyBranding`/`renderMotd`/`setLoginOrgName`, v0.50.0) · org logo · admins · watcher tokens · setup guide · init.

## app.py endpoint groups (grep the route to get the exact line)
- Nav/live: `/api/position`, `/api/state`, `/api/pois`, `/api/destination`, `/api/capture/*`, `/api/path/{action}`, `/api/refresh`. **Deep-space system ambiguity (v0.52.0):** every system's coords center on its own (0,0,0) in one numeric space, so raw deep-space positions can't name their system; `Session.system` (sticky, last container-confirmed) backfills `nav_state.system` and feeds `system_hint` through `custom_poi_from_position`/`observation_from_position`/`_frame_at` — never trust bare `system_at()` for deep-space stamps. **Container-name drift (2026-08):** stored custom POIs/observations anchor by container NAME + `local_km`; upstream feed revisions rename ('ARC-L1'→'ARC L1') and delete ('Wide Forest Station') containers, orphaning that data. `NavData.resolve_container` folds naming drift (`container_name_key`); `nav_core.register_ghost_containers` (called via `app._register_ghost_anchors` after the DB merges in BOTH nav-build paths) recovers upstream-deleted anchors from the code-bundled containers.json snapshot (`load_shipped_containers`, Dockerfile bundles it next to the code — the /data copy is the fetch cache and forgets). Ghosts are resolution-ONLY: never synth POIs, never `detect_container`, never in the belt registry. **Vendored starmap snapshots (2026-08-07):** `containers.json`/`poi.json` are committed reference data — NO runtime fetch (`load_nav_data` reads `_static_feed`, code-bundled copy shadows stale /data volumes; `/api/health` `source:"snapshot"`); re-sync = `tools/sync_containers.py` (reviewed diff report; deletions/true renames → `container_tombstones.json`, which feeds the ghost loader; station-class deletions flagged loudly) → commit → release. uexcorp feeds still refresh live
- Who's online (#19): `/api/online` (roster snapshot + `me` prefs), `/api/online/status` (set status/activity/appear-offline), `/api/playstyles` (shared activity/LFG vocab); LFG board `/api/lfg` (snapshot + post), `/api/lfg/{id}/join` (toggle), `/api/lfg/{id}` (close) — in-memory, WS `lfg` frame, surfaced in its own **Group Finder** app (`#/lfg`); `/api/lfg` snapshot carries `announce_available`; `POST /api/lfg` takes an `announce` flag → rate-limited Discord shout (`notify` category `lfg`, #19 step 4)
- Reference data: `/api/handles` (**projected to `{player_id, handle}`** — the registry entry's `discord_id` is the Discord↔handle link the directory opt-out exists to hide, so returning the raw entry to every signed-in member defeated it; the link is admin-only via `/api/intel/directory` + `/api/admin/handles`), `/api/commodities`, `/api/raw_commodities`, `/api/ships`, `/api/harvestables`, `/api/fauna`, `/api/resource_*`, `/api/biomes`, `/api/custom_pois`, `/api/observations`. **Ore value badges (#32):** `/api/resource_values` = per-category `{name: {sell, tier}}` (high|medium|low; rank terciles within category via `nav_core.resource_value_tiers`; raw→refined price fallback `_RAW_ORE_SUFFIX` in `build_resource_values`, fallback entries flagged `refined` → chip renders `$$$*` w/ basis tooltip; unpriced name absent = no badge; rebuilt on `/api/refresh`) → frontend `oreValues`/`oreValueChip`/`oreValueText` `$`-chips on forecast / NEARBY / element finder / destination / capture form+result. **RS signatures (Strata/CELD feed):** `/api/ore_signatures` = `{ores: {<our ore name>: {id, rs, tier, category, instability, resistance}}, locations, attribution}` from `$SC_NAV_DATA/ore_signatures.json` (`app.load_ore_signatures`, DATA_DIR only — **NOT committed**, unlike the wiki/starmap feeds: Strata states no redistribution license, so each deployment syncs its own). Built by `tools/sync_strata.py` off the key-gated strata.celd.space public API; **optional everywhere** — no `STRATA_API_KEY` = feature simply absent, card falls back to org scans. Deploy-time refresh = `app._sync_strata_feed()` startup task → subprocess (never an import: a hang can't take the server) w/ `--out $SC_NAV_DATA --max-age-h 24 --quiet`; the age gate is what keeps a restart loop from polling an API whose owners asked for per-patch caching. Bad key / missing tool / network failure all log and leave the existing feed alone. Key documented in `.env.example` + compose `${STRATA_API_KEY:-}`. Feed is a COMPLEMENT to the org's own GCD-derived `rs_bases` (`nav_core._survey_scan_stats`, untouched — it stays pure org evidence); the merge is presentational: frontend `oreSigs`/`oreSigKey` (suffix-tolerant, feed keys "Iron (Ore)" vs bare mark names) → `mergeRsRows` (datamined wins the base; org base that divides it = agreement `✓`, non-multiple = `!` clash) → FIELD `renderHaloRsRef` + zone `surveyRsCard` (names otherwise-unmatched `rs_seen` values off the reference). Attribution is a condition of use — the card renders it, don't drop it
- Cargo planner: `/api/route/plan|run|history|session/reset`
- Cargo analytics: `/api/cargo/leaderboard`, `/api/cargo/stats`
- Trade Route Planner (#21): `/api/trade/terminals|prices|trades`, `/api/trade/plan` (auto/filtered/manual); **run mode (step 5)** `/api/trade/run` (POST start / GET resume / PATCH `action` buy|sell|advance / DELETE abandon) + `/api/trade/run/replan` (re-solve from live position, sunk-cargo-aware). Legs not stops: per-leg buy→sell phase; solver in `nav_core.plan_trade_route`/`cost_trade_legs`/`replan_trade_route`; session helpers `_point_at_active_trade_leg`/`_advance_trade_run`/`Session.trade_run_view`. **History + stats (step 6):** `/api/trade/history` (personal runs + realized-profit stats + quick-picks), `/api/trade/session/reset`, `/api/trade/stats` (guild Trading section `#/intel/trading`, `#/trade-stats` aliased) + admin `/api/admin/stats/trade/clear`; realized profit `nav_core.trade_leg_realized`→`trade_run_realized`; derivations `derive_trade_run_stats`/`derive_trade_quick_picks`/`derive_guild_trade_stats`/`derive_trade_leaderboard`; frontend `renderTradeHistory` (#/trade RECENT TRADES) + `renderTradeStats`. **Favorites:** `GET/POST /api/trade/favorites` + `DELETE /api/trade/favorites/{id}` (`TradeFavoriteIn`; save plan *config* not resolved legs → re-solve on load; `trade_favorites` table; `db.list/save/delete_trade_favorite`); frontend SAVED ROUTES panel `renderTradeFavorites`/`saveTradeFavorite`/`applyTradeConfig`, shared `buildTradePlanBody` + new generic `promptDialog`. **Stock & demand reports:** every report has `side` supply|demand + `kind` out|low. Run-mode PATCH actions: `stockout` (buy-phase "⛔ no stock — skip & report", confirm-gated, skips leg) files supply-`out`; `demandout` (sell-phase "⛔ won't buy here — report & re-plan") files demand-`out` **without moving the cursor** (cargo stays aboard; flags `demand_reported`, client auto-replans); a confirmed buy OR sell with SCU < 50% of plan auto-files `low` on its side (`_LOW_STOCK_FRACTION`); `GET /api/trade/stock` (board + `ageoff_min`); `stock_reports` table (`db.stock_report_save/stock_reports_since/stock_reports_clear`, one live row per poi+commodity+side; `side` added via `_ensure_column` for v0.38.0 DBs), org setting `stock_ageoff_min` (default 180, pruned on read in `active_stock_reports`); fresh `out` reports drop their own side's solver candidates in plan/run/replan (`nav_core.stock_avoid_buys`/`stock_avoid_sells` → `_trade_candidates avoid_buys`/`avoid_sells`; demand-out also hard-drops held-cargo buyers in `_held_sell_leg`), `low` only badges (`nav_core.trade_leg_stock` matches supply→buy end, demand→sell end → leg `stock` views via `_annotate_leg_stock`); skipped/stockout legs get `skipped: true` and are excluded from realized stats (`nav_core._trade_sold_legs` + `trade_run_view`); frontend STOCK WATCH panel (`loadTradeStock`, side-aware labels), `reportNoStock`/`reportNoDemand`, `.tl-stock` plan badges, `stock-ageoff` ORG SETTINGS input. **Transaction capture (#41, docs/trade-transaction-capture.md):** the watcher parses commodity kiosk buy/sell lines out of Game.log (buy qty is centi-SCU; sell qty plain SCU; `parse_trade_txn` in `sc_nav_watcher.py`, on by default, `--no-trade-capture` sticky opt-out) → `POST /api/trade/transactions` (`require_user` = token OK, batch ≤20, natural-key dedup) → `trade_transactions` ledger; matching txn parks as `Session.pending_txn` → run view `txn` = the "⚡ terminal reported" confirm nudge (SPA prefills `.ta-price`/`.ta-scu`, `submitTradeLeg` auto-attaches `txn_id`); `PATCH /api/trade/run` w/ `txn_id` defaults actuals from the log + **learns `commodity_guids` (guid→name) + `shop_pois` (shop→poi) mappings** (`_consume_trade_txn`; first validated mapping wins) + files `price_reports` rows (#39 §6.1 ledger). Nudge cleared on any patch action/replan/abandon/run-start. NEVER auto-confirms: the log records the request, not fulfillment. **Cargo handling (§6):** the same lines state `autoLoading[0|1]` + `Cargo Box Data: boxSize|unitAmount` → watcher `_txn_cargo_handling` (separate optional searches; a reworded field costs the field, not the txn) → `TradeTxnIn.auto_load/box_size/box_count` → ledger cols (`_ensure_column`). **Captured, not yet read** — raw material for a dwell model to replace the method-blind flat `nav_core.STOP_DWELL_S` in every `profit_per_hour`; fit on BOX COUNT not SCU (cargo moves a box at a time), dwell measurable as txn stamp → departure off the position stream. Landed early because price can be re-derived and this can't. **Org price overlay (#39 slice 0, v0.87.0):** `_apply_org_price_overlay` (in `rebuild_trade_terminals`) + `_overlay_org_report` (live patch on report write) prefer the newest org observation over the UEX scrape per (terminal, commodity, side) — per-side `buy/sell_updated_at` + `buy/sell_src="org"` on price points → `nav_core._trade_row` passes them through → legs/board rows; `_price_fresh(side=)` judges freshness per side; ingest-time observations gated on both mappings ≥2 confirms (`_ORG_PRICE_MIN_CONFIRMS`); stockout/demandout also file `is_missing=1` ledger rows (overlay reader skips them — the stock board owns absence); frontend `⚡ org` accent badge in leg `.tl-fresh` + board `.sg-age`. **Stop kinds (#34, big-hauler filter):** `TradePlanIn.stops` / `TradeReplanIn.stops` = `any|stations|dock` (`_norm_stops`, `nav_core.STOP_MODES`). A Hull-C/D/E or Kraken has no landing gear — it can only moor at a station cargo dock. `load_trade_terminals()` now returns `(commodity, all_live)`; `nav_core.terminal_stop_kinds(nav, all_live)` → `{poi_id: {place: station|city|outpost, dock: bool}}` classifying over the **unfiltered** feed (a station states `has_loading_dock` on whichever desk UEX recorded it — Levski declares it on 'Cargo Services', never on its commodity desk — so OR the flag across all terminals at the POI) + a gateway-name rule (every jump gateway has a cargo deck; UEX omits the flag on the Nyx-side ones). Rebuilt in `rebuild_trade_terminals` → `app.trade_stop_kinds`; `nav_core.stop_exclusions(kinds, mode)` → `exclude_poi_ids`, a **separate** solver set from `avoid_poi_ids`: danger is negotiable (a held-cargo sell leg deliberately ignores `avoid_poi_ids` — you can run a blockade) but physics is not, so `_held_sell_leg` **does** honor `exclude_poi_ids`. Threaded through `_trade_candidates`/`plan_trade_route`/`replan_trade_route`; persisted in the run's `params.stops`. Manual legs are badged not dropped (`_annotate_leg_stops` → leg `no_stop`). `/api/ships` rows carry `dock` (uexcorp `is_loading_dock`: Hull C/D/E + Kraken ×2) → frontend auto-defaults STOPS to `dock` on ship pick (`tradeStopsForShip`, overridable). Frontend `#trade-stopkind` seg + `setTradeStops`/`TRADE_STOPS_HINT`; `_amenity_view` now keeps the wiki `Docking` amenity → `⚓ docking` chip. **Cargo legality (#42):** `TradePlanIn.legality`/`TradeReplanIn.legality` = `any|legal|illicit` (`_norm_legality`, `nav_core.TRADE_LEGALITY`) over UEX `is_illegal` (18/204) → `app.load_illicit_commodities()` (reads the COMMODITIES_FILE cache right after `load_commodity_names` refreshes it; global `illicit_commodities`, rebuilt in `_refresh_feeds`) → `nav_core.legality_allows` inside `_trade_candidates` (so plan + replan + continuation all covered; orthogonal to filtered-mode `commodities` — both apply). **Preference not physics** (mirror of #34): `_held_sell_leg` does NOT honor it, so switching to LEGAL mid-run never strands contraband aboard. Unlisted name = legal; empty flag set = ILLICIT matches nothing; bad value widens to `any`. `_annotate_leg_legality` sets leg `illicit` in EVERY mode (manual legs badged, never dropped) → frontend `.tl-illicit` ☠ chip on plan + run cards; `#trade-legality` seg + `setTradeLegality`/`TRADE_LEGALITY_HINT`, in `buildTradePlanBody` + `applyTradeConfig`; persisted in run `params.legality`. **⚠️ needs `wiki_pois_enabled`** — contraband's sellers are lawless outposts that only the wiki catalog places, so with it off every contraband BUY terminal fails the crosswalk (21/114 terminals resolve vs 114/114) and ILLICIT can't plan; `_explain_empty_illicit` says so instead of blaming the filters. **Sort by return + Stations & cities (#44):** `sort="return"` = third objective beside per_hour/profit (`optimize` in plan/replan, `_greedy_route`, `_route_score`, `rank_trades` sort key; board mirrors it via `boardSort`). **Greedy accept test:** route return is total_profit/peak_capital, so appending a bigger-buy_cost leg re-denominates the WHOLE route (200%→32%) — return mode takes a leg only if the running ratio improves, then stops; profit/per_hour can't lose by adding a positive leg, a ratio can. Narrow by construction (874% on 1,005 aUEC of Waste — maximizing a ratio ignores volume); tooltip says so. `stops="no_outposts"` = **Stations & cities**: `stop_exclusions` drops `place=="outpost"` only — `terminal_stop_kinds` already classified city (#34) but nothing consumed it, so `stations` was throwing the 5 big hubs (Lorville/Area18/New Babbage/Orison/Levski) away with the outposts. Live: 45 stations · 5 cities · 78 outposts; no_outposts plans 1.28M vs stations 825k. Unclassified still defaults to outpost (strict). **Profit margin + floor (#43):** `nav_core.trade_return_pct(buy_price, profit_per_scu)` = **return on capital (profit÷cost, NOT profit÷revenue)** — computed from PRICES so it's scale-invariant (well-defined before hold/budget/supply caps, no zero-SCU edge); `_trade_row` stamps leg `return_pct` (board gets it free), `_cost_route` summary `return_pct` = total_profit ÷ **peak_capital** (sequential trades recycle capital — summing buys understates); held leg → None (cargo already paid for, buy_cost 0). `min_return_pct` filters in `_trade_candidates` + `rank_trades` (threaded plan/replan/continuation), `TradePlanIn`/`TradeReplanIn` + `/api/trade/trades?min_return_pct=`, `_norm_return_pct` (0/neg → None), persisted in run `params.min_return_pct`; manual legs badged `low_return` via `_annotate_leg_return`, never dropped. **Do NOT merge with the board's older `min_margin`** — that's an aUEC/SCU spread, this is a percentage; different questions, both apply. Frontend `#trade-minreturn` input (beside max spend), `.tl-return` leg chip + RETURN summary metric + board row %. Solver still optimizes profit/hour — the floor is a CONSTRAINT, so raising it buys percentage with absolute money (200% floor → 874% return on 1,005 aUEC of Waste)
- Pirate danger warnings (#24): `/api/warnings` (board snapshot + `announce_available`), `POST /api/warnings` (post point|lane, pvp|pve, severity, anchor POI id(s) + free-text location; opt-in `announce`), `POST /api/warnings/{id}/confirm` (community "still active" refresh), `DELETE /api/warnings/{id}` (poster/admin). In-memory `Hub.warnings` board persisted to `pirate_warnings`, time-based age-off (`warning_ageoff_min`/`warning_stale_min`, default 60/40) pruned in the presence broadcaster, WS `warnings` frame; `notify` category `pirates`. Frontend = its own **Danger Board** app (`#/pirates`): composer + board + launcher card + `☠️ N` badge; admin lifecycle knobs `warning_ageoff_min`/`warning_stale_min` in ORG SETTINGS. **Trade-planner integration:** `avoid_mode` (ignore|warn|avoid) on `TradePlanIn`/`TradeReplanIn`; `hub.active_trade_warnings()` → `_solve_trade_plan`/replan build avoid sets (nav_core `trade_avoid_sets`) for the `_trade_candidates` `avoid_poi_ids`/`avoid_pairs` filter, and `_annotate_trade_legs` (nav_core `trade_leg_warnings`) tags touched legs; frontend `setTradeAvoid` seg + per-leg ⚠ badge + route-level callout in `renderTradePlan`. **Board→events:** each card's "⚔ Organize hunt" (`promoteWarningToEvent`) prefills CREATE EVENT via the shared `eventSeed` (was `lfgEventSeed`) + `#/events/new` + `POST /api/events`. **v2 snare-detour routing (docs/snare-detour-routing.md):** warnings become hazard *volumes* (`nav_core.hazard_volumes` → sphere/capsule; base `hazard_radius_km` org setting ×severity), `travel_cost(avoid=, memo=)` decomposes a leg into jump segments (`_leg_segments`), tests them (`segment_hits`/`_seg_point_dist`/`_seg_seg_dist`) and inserts a detour waypoint (`_detour_via`) or flags `blocked`; result carries `waypoints`/`detour_m`/`dodged`/`blocked`. Threaded through the trade solver + cargo `plan_route(avoid_volumes=)`. `avoid_mode` **default flipped to `avoid`**; both `TradePlanIn`+`RoutePlanIn` gain `avoid_poi_ids` (personal blacklist, localStorage `avoidPois`, shared by both planners via `mountAvoidBlacklist`); `RoutePlanIn.avoid_mode` new (cargo had none). `_build_hazard_volumes` in app.py; warn-mode fly-past via `nav_core.leg_hazards`; cargo stops via `_annotate_cargo_stops`. Frontend: `detourVia`/`waypointSteps`/`worstSev` helpers, "dodge via" spans + blocked lines + `.route-reroute` callout, live `maybeTradeRerouteNudge` banner on a new-danger WS frame
- Prospector / Halo Finder (#31 docs/halo-finder.md; **multi-system #35** docs/halo-finder-expansion.md; **#38 app restructure docs/survey-app-restructure.md: renamed Prospector, 3 masthead tabs DROP `#/halo` · FIELD `#/halo/field` (armed-plan pin + verdict + radar + ⛏ mark; nudge dot on new fix) · ATLAS `#/halo/atlas` (zones list/detail + coverage map + export); system seg in the masthead; frontend-only**): `GET /api/halo/targets?system=` (per-system drop-target catalog: Stanton `bands`, Nyx `ring`+`pockets` (381 datamined Glaciem segments, only ~4% of the ring holds rocks → aim at pocket centers), Pyro `fields` (102 unmarked resource fields incl. Akiro=PYR1-L3) + map `bodies` + per-system attribution; `/api/halo/bands` = Stanton-shaped alias), `POST /api/halo/plan` (`HaloPlanIn`: goal picks system — `band` 1–10 (Stanton) XOR `field_uuid` (Pyro) XOR bare `system:"Nyx"` ring/`pocket_key` pin (+`include_mission`) XOR `target_poi_id` (any system); `start_poi_id` or live fix; `aim` band|peak; `allow_staging`; ship/qd; shared `avoid_poi_ids`) → `{system, mode: band|pocket|poi, start, band|target, staged, legs, drop (+drop.pocket), alternates, attribution}` (alternates = full drop+leg pairs at distinct markers, client promotes without re-plan), `GET /api/halo/locate` (classify latest fix per its system's belt: Stanton band/void/outside · Nyx pocket/ring_void/off_ring · Pyro field/space; `?target_poi_id` adds miss — the Refine loop). Registry `nav.belts` = `nav_core.build_belt_registry` (built in `_apply_wiki_catalog` ALWAYS, independent of the POI toggles; Nyx pockets derived from `AsteroidBelt` containers, Pyro fields from the wiki feed). Solver `nav_core.plan_halo_drop` (band/POI/pocket modes; pocket staging falls back to smallest-near-miss stage + 0.6× rule) + geometry (`HALO_BANDS`, `_ring_crossings`, `halo_band_crossings`, `halo_locate`/`glaciem_locate`/`field_locate`, `halo_contains`+`glaciem_contains` ring-envelope system discriminators in `system_at`, `body_volumes`+`chord_obstructed`) at the end of nav_core.py. **Deep-space ladder (#35):** `Session.system_t` freshness stamp (`SYSTEM_STICKY_FRESH_S` 30 min) → `_halo_fix_system`: container > belt geometry unless a FRESH sticky contradicts it > sticky > guess. Capture flow belt-annotates POIs (`_halo_capture_note`; in-pocket captures via the detected ring-segment container); navigator chip `haloWhereChip` (Stanton band + Glaciem pocket/ring). Frontend: `#halo-system` seg STANTON|NYX|PYRO, per-system target panels (band strip · pocket AUTO+datalist pin+mission toggle · field combobox grouped by shell), per-system `drawHaloMap`/`drawHaloInset` (ring + pocket dots / pocket grid-sphere inset), per-system verdicts + credit line. NOT in-game verified (design doc §7). **Belt survey (#36, docs/belt-survey.md):** Keeger Belt (48 Gm, no container geometry — org maps it): survey marks = custom POIs `type="survey"` + `custom_pois.survey` JSON col (`SurveyPayloadIn` rocks none|sparse|medium|dense / ores / salvage; negatives first-class) via `/api/capture/start`; `nav_core.survey_marks/survey_pockets` (tier-1 clusters LIVE from mark #1, `SURVEY_MERGE_M`, negative-capped extents, keys `SVY-<id>`) + `survey_field_model` (tier-2, gate 25) + `keeger_contains`/`keeger_locate`; **keeger is a guarded system-disambiguation rung** (system_at + `_halo_fix_system` — hint-less 48 Gm fixes otherwise stamp "Stanton"); `POCKET_MISS_CEILING_M` 100,000 km rejects un-plannable deep-belt marks w/ contract-marker 400 (plannable sweet spot = station approach chords); `HaloPlanIn.belt` glaciem|keeger (SVY keys pin directly); **datamined Keeger arcs (#36 phase 1, 2026-08-07):** the 30 upstream-relocated mission segments = `nav_core.keeger_segments` (geometric claim: Nyx AsteroidBelt ∧ `keeger_contains` ∧ ¬`glaciem_contains`; belt-qualified `KGR-*` keys via `keeger_segment_key` — the bare trailing key names a TOMBSTONED Glaciem pocket at 15 Gm) → registry `segments` + targets doc `segments` (w/ `pocket_reach`); pin-ONLY, never any AUTO pool; measured: NO marker chord within 0.59 Gm of any arc, so a KGR pin 400s w/ contract-access guidance (tailored branch in `plan_halo_drop`'s empty-pool rejection); `keeger_locate(segments=)` annotates verdicts (`segment` field, org pocket stays primary — surveyed-by-players is the theme) + `_halo_capture_note` "datamined arc KGR-…" so ⛏ marks inside arcs ARE the contract-gating verification record; frontend violet outlined+labeled arcs on the maps + datalist rows + caption legend; `GET /api/halo/survey`+`/export` (versioned `_meta`), `POST /api/admin/survey/clear`; Nyx targets doc gains `keeger`/`surveyed`/`survey` blocks; frontend ⛏ survey block in AFTER-THE-DROP (density seg + ores + salvage, ws-resolved via `resolveHaloSurveyArm`), GLACIEM|KEEGER belt seg + progress + export link, org-survey badges, keeger dashed ring + surveyed dots on the map. **Survey stats + milestones + nudge (#37 slice 5, §5.2–5.3):** `notify` category `survey` — `ZoneIn.announce` (LFG-style opt-in, `_survey_announce_ok` cooldown, `announce_available` on the zones snapshot + create-dialog checkbox via `promptDialog` `checkbox` opt) + threshold-crossing milestones in `_capture_poi` → `_survey_capture_milestones` (zone hits gate 25 positives / belt's first `survey_field_model` fit; fires ONLY when count == gate exactly; private marks never); client-only radar drift nudge `radarMarkAt` (set on ⛏ resolve, `#halo-radar-nudge` when >½ envelope radius from last mark)
- Events: `/api/events*`, `/api/events/{id}/signup`; **fleet roster (#20)** `/api/events/{id}/groups[/{gid}]` (board + group CRUD), `/api/events/{id}/assignments` (PUT assign/move/unassign, group_id null = unassign), `/api/events/{id}/manifest` (+ `/post` → Discord, now chunked on group boundaries via `_manifest_chunks` ≤1800 chars/msg; manifest header carries the start `<t:>`). Plan is organizer/admin-owned; nav-side logic `nav_core.derive_roster_board`/`build_event_manifest`. **2026-08 org-launch batch:** `max_players` ENFORCED — full event waitlists `going` joiners (signup status `waitlist`, first-come by signup `created_at`; withdraw auto-promotes via `_promote_from_waitlist` + 🎟️ ping `_notify_waitlist_promoted`; `waitlist_count` on views, waitlist never inflates `fill`); reschedule handling in `edit_event` (start change → `db.reset_event_reminder` re-arms the loop + `_notify_event_rescheduled` pings signups; place change pings too); `PUT /api/events/{id}/attendees` (organizer/admin day-of seat/drop, skips signups-open + capacity gates, `AttendeeIn`); `POST /api/events/{id}/complete` (`db.complete_event`, scheduled-only → past tab regardless of clock); cancelled-but-FUTURE events stay on the upcoming board badged (db.list_events includes them; past scope ORs `status='completed'`); board UI: Upcoming|Past seg + "My events" toggle + category chips (`evBoardRange/Cat/Mine`) + `✓ going`/`⏳ waitlisted` card badges (`ev-mine-chip`); Clone button (`cloneEvent` → `eventSeed`, past start rolls +7d); organizer `Manage attendees` disclosure (`organizerAttendeePanel`/`bindAttendeePanel`); fleet `saveGroup` round-trips `notes`/`parent_id`/`sort` on edit (PATCH full-replace used to wipe them); P0 FIXED: detail roster `attendeesByRole` indexed `byRole[t]` with the `{role,needed}` OBJECT — target roles always rendered "— open —"
- Resource manager: `/api/catalog`, `/api/inventory*`, `/api/goals*`. **Contributions
  are reversible pledges (v1.2, docs/org-inventory-goals.md §v1.2):** a holding may be
  committed PAST its qty (promising to gather is legit) — the uncovered part is derived,
  never stored (`db._ALLOC_SHORT_SQL`, oldest-allocation-first attribution) → contribution
  rows carry `short` → `nav_core.derive_goal_progress` adds per-line/per-contributor
  `promised`/`on_hand` (pledges still count toward `have`). `POST …/contribute` gains
  `holding_id` (which stash), `on_hand` (declare + top up the holding; default OFF, so a
  pledge no longer invents stock — it hangs off a qty-0 shell that `derive_inventory_rollup`
  skips) and `allow_over`; over-filling a line 409s with the confirm prompt as `detail`
  (server-side ON PURPOSE — the race is two members on a stale board). Withdraw =
  `PATCH|DELETE /api/goals/{id}/contribute/{alloc_id}` (contributor-or-admin; empty
  shell holdings swept). `PATCH /api/inventory/{id}` no longer blocks qty < committed.
  Frontend: goal detail source picker + MY CONTRIBUTIONS (grouped by location,
  `goalMyContribs`/`postContribution`/`withdrawContribution`), hatched pledge bar
  segment, `⏳ gathering` annotations on lines/chips/inventory rows + `.rm-alloc-x`
  withdraw in the holdings table. **Craft-goal spec (#14.2):** `GoalIn.blueprint_key/qty/inputs` (per-slot quality asks, `SpecInputIn`) → seeded line items carry `min_q` = max(recipe, ask) (`nav_core.blueprint_goal_lines input_qs=`); persisted `goals.blueprint_qty`/`blueprint_inputs` (JSON, `_ensure_column`); `_goal_craft_block` returns qty/inputs/`stat_preview` + min_q map; hand-edited lines never touch the stored spec (edit re-seeds only when bp fields sent w/o lines); craft block + `/api/blueprints/{key}` carry `est_cost` (per-craft mats estimate)
- Marketplace: `/api/market*` (offers, confirm). **Modes: sale | auction | barter | commission (#25 craft requests)** — commission = "build me this, to this spec, for this price": poster stays `seller_id` ("Requester"), accepted crafter = `buyer_id`; quote = offer w/ required amount, never instant-deal; accepted crafter withdraw → listing back to `open`; lazy needed-by expiry (no winner); `listings.blueprint_key` + `listings.materials` (requester|crafter|split) via `_ensure_column`; spec under `attributes.spec` (CraftedIn shape); denorm `sort_price` = lowest quote else budget; `nav_core.commission_board_state`; **opt-in `announce` works on EVERY mode** (was commission-only): `_notify_listing_posted` picks headline+CTA from `_LISTING_ANNOUNCE_COPY` (🏷️ FOR SALE · 🔨 AUCTION · 🔄 TRADE WANTED · 🛠️ WANTED) + mode terms from `_listing_announce_terms`; ONLY commission @-mentions library-matched crafters (`db.blueprint_crafters`, cap 15, poster excluded) — a crafted *sale* carries a `blueprint_key` too but must not ping. Cooldown scales with loudness (`_listing_announce_ok(poster, mode)`: `COMMISSION_ANNOUNCE_COOLDOWN_S` 600s pinging vs `LISTING_ANNOUNCE_COOLDOWN_S` 60s quiet); create response carries `announced` so a swallowed shout isn't reported as sent (frontend one-shot `mktPostNotice` → `.mk-post-note` on the detail). Form checkbox is create-only + gated on the board's `announce_available` (`mktAnnounceAvail`/`syncMktAnnounceAvail`); **CSS trap fixed: `.on-invis` set `display:flex`, which beats the UA `[hidden]` rule — the LFG + danger-board announce rows had the same latent bug, now covered by `.on-invis[hidden]`**; commission views carry `mats_est` (`_commission_mats_est`, board card + detail price line). **Crafted-sale identity (#25.1 §11.3/§11.4):** ANY listing whose item is `blueprint:<key>` stamps `blueprint_key` (sale/auction too) → `kind=blueprint` board filter finds crafted goods; detail view carries `expected_stats` (`_listing_expected_stats`: commission spec.inputs → basis `inputs`, else advertised overall quality → basis `uniform`) + `mats_est`; frontend `mkExpectedStats` panel + manifest panel on any blueprint-linked listing + ⚒ crafted rows in the item picker (`attachCatalogPicker` opts.blueprints) + mode-aware copy in the 4 `_notify_market_*` helpers. **2026-08 org-launch batch (docs/org-launch-review-2026-08.md):** lazy settlement now NOTIFIES (`_resolve_listing_expiry` → `_notify_auction_settled` "you won"/no-bids + `_notify_commission_expired`; winner matched by max-not-float-equality — old code could leave pending w/ NO accepted offer); outbid ping (`_notify_outbid` in `place_offer`); cancel-with-commitments + crafter-bail + commission-quotes-lost pings; offer action `decline` (seller/admin, non-auction UI, → `rejected` + quiet ping); `market_sweep_loop` (60s, idle w/o webhook) proactively settles + fires ⏳ ending-soon 1h out (`_notify_listing_ending_soon`, once-per-process `_ending_soon_warned`); auction `ends_at` extend-only once bids exist; board `activity=me` ("My activity" tab: live offers + pending deals, per-card `my_stake` w/ outbid/confirm badges) + `craft=1` (server-side can-craft, replaced page-local filter) via `db.list_listings(bidder_id=, craftable_by=)`; staleness: org setting `listing_stale_days` (default 14, 0=off, `_listing_stale_days`) → board `stale_days` → `mkStaleBadge` off `updated_at` + `POST /api/market/{id}/renew` (`db.touch_listing`, "↻ Still available"); admin moderation `DELETE /api/market/{id}` (hard-delete); Relist (seller, closed listings → `renderMarketRelistForm` seeds create form); offers list carries `bidder_deals`; live countdown ticker (`mkCountdownText` + 30s interval on `.mk-ends[data-ends]`) + refetch-on-visibilitychange (>60s hidden)
- Blueprint feed (#25/#26): `GET /api/blueprints` (search index `?q`/`?category`, cap 50) + `GET /api/blueprints/{key}` (full record + derived `manifest`/`stat_drivers`); committed `poi/blueprints.json` from `tools/sync_blueprints.py` (SC Wiki API, re-run per game patch); `blueprint:<key>` catalog namespace resolves in `resolve_catalog_item`; `/api/catalog?bp=1` appends recipe matches (marketplace picker ONLY — inventory/goals pickers stay recipe-free); `GET /api/blueprints/stat-names` (canonical ~25-stat vocabulary, registered before `/{bp_key}`; datalist autocomplete on crafted-stat rows, `mkFillStatNames`); `est_cost` = `nav_core.blueprint_material_cost` × `_blueprint_price_of` (item_prices buy-side; resources only, gems/items degrade to `unpriced`) → "mats ≈" line in `bpMatsCost`/`bpManifestHtml`; nav_core `blueprint_manifest`/`blueprint_stat_drivers`/`blueprint_quality_effect`/`blueprint_stat_preview`; frontend spec builder = shared `bpSpecCtl` controller (instances `mkSpec` market form / `goalSpec` goal form; sliders + materials bill + stat estimates) + `attachBlueprintPicker` + JS twin `bpEffectAt`; goal detail `goalSpecBox`
- Org analytics: `/api/leaderboard`, `/api/stats`, `/api/intel/directory`, `/api/intel/surveying` (#37 slice 5: totals + ranked members via `nav_core.derive_survey_stats` — sessions derived from mark stream, gap `SURVEY_SESSION_GAP_S` 30 min or zone change splits, NULL-`created` never sessioned — + per-belt coverage rows + freshest zones; frontend `#/intel/surveying` → `#survey-stats-view` sibling, `loadSurveyStats`/`renderSurveyStats`)
- Admin: `/api/admin/stats/*/clear`, `/api/settings`, `/api/org-logo`. **Guild
  branding (org name + MOTD):** `/api/settings` carries `org_name` (helper
  `org_name()`) + `motd` (helper `motd_state()` → `{text, updated}`); saving new
  MOTD text stamps `motd_updated` epoch (no-op re-save keeps it so dismissals
  don't resurface). Public **`GET /api/branding`** (`{org_name, org_logo}`,
  auth-gate-exempt) feeds the pre-auth login splash; `GET /api/me` carries
  `org_name`/`motd`/`motd_updated`. Frontend: login-splash + launcher
  `login-org-name`/`launcher-org-name`, dismissible `#motd-banner` on the
  launcher (per-member dismissal keyed to `motd_updated` in localStorage
  `motdDismissed`), `applyBranding`/`renderMotd`/`setLoginOrgName`, ORG SETTINGS
  BRANDING name input + MESSAGE OF THE DAY panel. POI-catalog
  toggles in `/api/settings`: `starmap_pois_enabled` + `wiki_pois_enabled` (#28,
  both default OFF, flip → `_rebuild_nav`). **Scheduled UEX feed refresh
  (#33):** `feed_refresh_h` org setting (default 6h, HARD 2h floor
  `_FEED_REFRESH_FLOOR_H` — POST rejects <2, reader clamps up; 0 = off) drives
  `feed_refresh_loop()` (startup task, re-reads setting per 5-min tick, OFFLINE
  no-ops) → shared `_refresh_feeds()` (all uexcorp feeds + item_prices +
  resource_values + catalog + terminal crosswalk + re-`enrich_ships_quantum`;
  feeds-only — the starmap catalogs are vendored snapshots, release-updated via `tools/sync_containers.py`). `/api/refresh` = `_refresh_feeds` +
  `_rebuild_nav`; `feeds_refreshed_at` epoch on `/api/settings`; ORG SETTINGS
  "UEX PRICE DATA" panel (interval + "prices as of" + Refresh now button). **Wiki locations catalog (#28,
  docs/wiki-poi-enrichment.md):** committed `poi/locations.json` from
  `tools/sync_locations.py` (SC Wiki API, re-run per game patch, CC BY-SA 4.0);
  `app._apply_wiki_catalog` in `load_nav_data` → nav_core `add_wiki_pois` (ids
  4M+, `Poi.source="wiki"`, token-name dedup via `wiki_name_key`, starmap wins)
  + `upgrade_qt_markers` (promotes matched starmap POIs the game marks
  `qt_valid`) under the toggle, `annotate_arrival_radii` always (→
  `Poi.arrival_radius_m`, used by `_arrived_at_active` ×1.5 w/ 10 km floor
  instead of flat `ARRIVAL_SPACE_M`); trade-stop amenity chips
  `_amenity_view`/`WIKI_AMENITIES`/`_annotate_leg_amenities` → leg
  `buy_amen`/`sell_amen` → frontend `amenChips` (plan + run views), ORG
  SETTINGS `wiki-toggle`. **POI quality control (admin overrides):** flag a bogus
  imported POI **bad** (excluded from routing + search + destination + halo target)
  or force its **QT-marker** on/off. `GET/POST /api/admin/poi-overrides` +
  `DELETE /api/admin/poi-overrides/{id}` (`PoiOverrideIn`: `poi_id`→resolves the
  stable key, `bad`, `qt_override` None|0|1; no-op clears the row). Keyed by the
  STABLE natural key `nav_core.poi_override_key` (`<system_lower>::<wiki_name_key
  tokens>`) NOT the numeric id — starmap item_ids shift per patch + wiki dedup keeps
  the incumbent id, so ids don't survive a re-import but the name-key does. Applied
  by `nav_core.apply_poi_overrides` in the merge pipeline **after**
  merge_custom_pois/observations, **before** `assign_qt_markers` (both `_rebuild_nav`
  AND startup), so a fix outlives every import. Two new `Poi` fields: `disabled`
  (→ `nav_core.poi_active` guard in `index_qt_markers`/`search_pois`/`compute_state`
  destination + `/api/destination`/halo target reject) and `qt_marker_base` (per-object
  imported-QT snapshot → clearing a QT override reverts with no feed reload). Live
  writes re-apply via `_apply_poi_overrides_live` (apply + `assign_qt_markers` under
  `hub.lock`, clears any session aimed at a now-bad POI). Frontend = ORG SETTINGS
  "POI QUALITY CONTROL" panel (`#poi-qc-panel`: `/api/pois?q=` search rows w/ flag-bad
  + QT `.seg` tri-state, ACTIVE OVERRIDES list w/ live match-count + Clear;
  `loadPoiOverrides`/`renderPoiQcResults`/`poiOverrideSet`). Name-key collisions
  intentionally apply to all siblings. Trade-terminal feed + NEARBY list out of scope.
- Auth/account: `/auth/login|callback|logout`, `/api/me*`, `/api/tokens`. **Member profile (#30):** `members.playstyle_tags` (JSON via `_ensure_column`, `db.set_member_playstyles`); `PUT /api/me` `playstyle_tags` (allowlist `PLAYSTYLE_TAGS`, dedup, cap `_PROFILE_MAX_TAGS`=6, parses via `member_playstyles`, mirrors onto the live online record + roster rebroadcast); carried on `GET /api/me`, `/api/intel/directory` rows, and online-roster records (`tags`); UI = Settings PROFILE chips + roster/directory `.on-ptag` chips. **Ping opt-out (2026-08):** `members.notify_opt_out` (`db.set_notify_opt_out`/`notify_opted_out`) honored in `_mentions()` + `_event_attendee_ids` (name stays in message text, id leaves allowed_mentions); `ProfileIn.notify_opt_out` on `PUT /api/me`, carried on `/api/me`; Settings PROFILE "Don't @-ping me" checkbox. **In-game handle binding (2026-08-05):** `handles.discord_id` is bound trust-on-first-use by `_bind_handle` (shared by `POST /api/position` and the new `POST /api/handle`), which also sets `Session.owner` / `Session.handle_conflict` and returns a verdict `{handle, bound, conflict, player_id}`. Three fixes to a binding that *looked* broken: (1) **`POST /api/handle`** (`HandleIn`, `require_user`) binds with NO position fix — binding used to ride `/api/position` alone, so a member with the watcher up + `--handle` set still saw "no watcher-bound handle yet" until their first in-game `/showlocation`; the watcher pings it at startup (`HANDLE_ENDPOINT`/`Sender.register_handle`, skipped w/o a token) and it pushes a WS state frame so an open tab updates instantly. (2) The Settings panel was a **boot-time `/api/me` snapshot** (`renderAccount` runs once) — now `refreshIdentity()` re-reads it on entering `#/settings` and once per newly-bound handle seen on a state frame (`noteHandleBind`, one refetch per handle via `identityBindSeen`). (3) The **anti-hijack refusal is no longer silent**: a handle already bound to a different account keeps `owner` None (deliberate — it would otherwise let anyone claim a member's captures), but now sets `Session.handle_conflict` → `capture_status().handle_conflict` → navigator credit line + `#handle-conflict-note` `.handle-warn` banner, returns `conflict:"owned_by_other"` to the watcher (`Sender._note_handle` logs once per change), and prints a server log line. An all-whitespace handle is rejected (400) instead of registering as `""`. **Admin escape hatch:** the one-way guard means a handle bound to the wrong account is otherwise stuck forever (SQL was the only fix) — `GET /api/admin/handles?q=` (rows carry `owner_name`/`owner_known`/`blocking`; `blocking` = live sessions whose `handle_conflict` names it, i.e. exactly who is being refused right now, and those rows sort first) + `DELETE /api/admin/handles/{player_id}/owner` → `handles.set_owner(pid, None)` (the ONLY path that may move a binding) → `db.set_handle_owner`, then `_apply_handle_binding_live` drops `sess.owner` on the session that held it and clears now-stale `sess.handle_conflict`s. Clearing keeps the PlayerID, so existing captures keep their attribution; it just re-arms trust-on-first-use. Frontend = ORG ADMIN **IN-GAME HANDLE BINDINGS** panel (`#handle-bind-panel`, `loadHandleBindings`/`renderHandleBindings`/`unbindHandle`, confirm-gated, loaded from `loadSettings`)

- Discord notify plumbing (server/notify.py, 2026-08 upgrades): `send(embed=)` — webhook-native embeds, no bot; **every events + marketplace builder (18) now sends an embed**: app-palette colors (`_EMBED_INFO/GOOD/WARN/BAD/MUTE` in app.py), `_embed()` helper + `_app_url()` (embed url = deep link; None w/o SC_NAV_PUBLIC_URL), `content` carries ONLY the `<@id>` pings (mentions inside embeds never ping), user names ride embed TITLES (no markdown render → no `**` injection garbling), `<t:>` tags never in titles (don't render there), manifest post stays plain text on purpose (chunked document); goals/records/lfg/pirates/survey builders still plain. Dispatcher: embed-only messages valid (empty content key omitted; fully-empty message → False), title 256/desc 4096/field 1024 caps. `send_paged(body, mentions=, embed=)` — chunks >50 mentions across messages (Discord's allowed_mentions cap; body truncates BEFORE pings, continuation pages dedup as `key:pN`, embed rides page 0 only) — used by event reminder + reschedule; per-category delivery health `_health`/`_note_result` → `webhook_status()` carries `last_ok/last_error/last_error_at/fails` → red "Last delivery failed" line in the ORG SETTINGS webhook rows (a revoked webhook 404s forever, used to look green); `_notify_bg` keeps strong task refs (GC could eat fire-and-forget tasks) + no-running-loop guard; org setting `listing_stale_days` rides `/api/settings` (MARKETPLACE LISTINGS panel). Test conv: `_capture` fixtures take `**kw`; `test_app._msg_text(text, embed)` flattens content+embed so assertions target the whole rendered message. **Send pacing (D3):** per-category slot reservation `_pace`/`SEND_SPACING_S` 2.0s (≤30 msg/min = Discord's webhook cap; reservation is synchronous between awaits → atomic, no lock) + 429 retry_after honored to 30s; payload-level tests set `SEND_SPACING_S = 0`. **Org price memory (B7):** `db.item_deal_prices` → `_org_price_memory(item_id)` = per-UNIT `{count, last, median, last_at}` from completed deals' `final_auec`; on `_listing_view` detail (`org_price`) + `GET /api/market/item_history?item=` (registered BEFORE `/{listing_id}` — path-param would 422-swallow it) → detail "⚖ Sold in-org before" line + create-form `mk-orghist` hint w/ use-button (`mkShowOrgHistory`). **Availability + pickup (B8):** `listings.availability` (`_LISTING_AVAILABILITY` instock|pickup|ondemand|scheduled, `_norm_availability`) + `listings.pickup_poi` (`_ensure_column` both, in create/edit/PUBLIC/CARD), board `?avail=` filter (`db._listing_filter_sql avail=`); frontend `MK_AVAIL`/`mkAvailChip` chips, detail handoff line, form select + POI-picker pickup field, `.mk-availsel` board filter. **Competitor-ideas batch (2026-08-05, all 6):** (1) **WTB buy orders** — 5th `_LISTING_MODES` mode `wtb` ("I want X, paying Y"; poster = paying side in `seller_id` like commission, accepted responder → `buyer_id`; NEVER instant — stock unverifiable; `sort_price` = best (lowest) offer else paying price, shared commission branch in `_recompute_denorm`); board tab BUYING, form "Paying (aUEC)" `mk-f-paying`, detail respond block `mk-sell`, `mkRoles` wtb = Buyer/Seller, announce copy 📥 BUYING; (2) **WTB inventory match** — `db.item_holders` (RM inventory, qty>0, biggest first, cap 15) → `_notify_wtb_match` directed ping on wtb create (independent of announce; the match only an org-internal market can make); (3) **view counters** — `listings.views`, bumped on non-seller detail GET, `view["views"]` seller-only (anonymous count, not a public popularity score); (4) **trends** — `GET /api/market/trends` (`db.market_trend_rows`: hot = 30d settled per-unit medians, most_listed = open sell-side, buy_orders = open wtb) → board `#mk-trends` disclosure (lazy on toggle, `loadMarketTrends`); (5) **profile notes** — `members.availability_note` ("usually on: …", rides `seller_availability` on listing detail + crafter rows) + `members.crafter_note` (non-empty = storefront ON) via `db.set_member_note`/`members_dir.set_member_note`, `ProfileIn` fields, Settings PROFILE inputs (`noteField` helper); (6) **crafter storefronts + directed commissions** — `GET /api/market/crafters` (`db.list_crafter_storefronts` + deals + availability), board `#mk-crafters` panel on the Requests tab (`loadCrafterPanel`, "Request a craft" → `mktFormDirect` seed → create form pre-set commission + `directed_to`); `listings.directed_to` (commission only, snowflake-validated): ONLY the target may quote (403 in `place_offer`), `_notify_commission_directed` dedicated ping, announce never mass-pings crafters on a directed job, detail 🎯 line (`directed_to_name`/`is_directed_to_me`). **Directed-picker scoping (user-found gap, 2026-08-05):** `GET /api/blueprints?crafter=<id>` filters the index to that member's LIBRARY; the form's bp picker reads `wrap.dataset.crafter` (set when `mktFormDirect`) → focus-to-browse their whole list (slice 50 when scoped); create 400s when the directed recipe isn't in the target's library (`db.member_has_blueprint` dead-letter guard); storefront rows' "⚒ N recipes" = `.mk-bplist-tog` expander (lazy chip list; `.mk-crafter-bps[hidden]{display:none}` — the display:flex-beats-hidden trap AGAIN); **"who can craft X" filter**: `GET /api/market/crafters?blueprint=<key>` (`db.list_crafter_storefronts(blueprint_key=)` EXISTS clause) → panel recipe-picker (`mktCrafterBp`, `#mk-crafterbp` + clear chip; filtered-empty state points to the whole board); an active filter rides into "Request a craft" (`mktFormDirect.bp`) → the form arrives with crafter AND recipe pre-picked (spec builder loaded via `mkSpec.load`)
- Update check (self-hosting orgs): `GET /api/updates?force=` (admin-only) polls
  `api.github.com/repos/{UPDATE_REPO}/releases/latest` (anon; releases are published
  per version by `.github/workflows/tag-release.yml`) and compares against
  `version.py` via `update_available`/`_semver_parts` (numeric, not lexical; an
  unparseable tag reports "no update"). Module-global `_update_cache`, 6h TTL +
  300s force floor (anon GitHub API = 60 req/h/IP), fetch in `asyncio.to_thread`;
  a network failure caches the error string and still 200s. Repo from
  `SC_NAV_UPDATE_REPO` (regex-validated `owner/name`; blank OR `SC_NAV_OFFLINE`
  = check off, `enabled:false` + `disabled_reason`) — compose passes it with a
  single `-` default so unset ≠ empty. Frontend = ORG ADMIN **SERVER VERSION**
  panel (`#version-panel`, `loadUpdateCheck` called from `loadSettings`,
  `.upd-banner` — `[hidden]` rule required, it's a flex). Read-only by design:
  never self-updates.
- Misc: `/api/health`, `/api/branding` (public org name + logo flag), `/download/watcher`, `/` + `/index.html`

## db.py tables
meta · custom_pois · poi_overrides · observations · handles · members · watcher_tokens ·
user_ships · runs · trade_runs · trade_favorites · stock_reports ·
trade_transactions · commodity_guids · shop_pois · price_reports (#41/#39 §6.1) ·
pirate_warnings · events ·
event_signups · event_groups · event_assignments · group_templates · lfg ·
catalog_items · inventory · goals · inventory_allocations · listings ·
listing_offers.

## Guardrails (don't regress these)
- **Security**: CSP/nonce + defense-in-depth headers (app.py `_csp`, http middleware);
  WS origin check; image magic-byte sniff (`_sniff_image`, no SVG); input caps on
  Pydantic models. Don't widen these casually. Full posture + threat model:
  `docs/security-review-2026-08.md`. The load-bearing pieces:
  - **`auth_gate` is deny-by-default.** `_PUBLIC_EXACT` (method+path) +
    `_PUBLIC_PREFIXES` (`/auth/`, `/images/`) are the ENTIRE anonymous surface;
    everything else needs a session. A new route is private unless you list it.
    It used to be "deny `/api/*`, allow the rest", which silently published
    `/openapi.json`, `/docs`, `/redoc` and anything dropped in `server/static/`.
    Interactive API docs are off (`FastAPI(docs_url=None, ...)`).
  - **Watcher tokens are scoped** to `_WATCHER_PATHS` (`/api/position`,
    `/api/handle`, `/api/trade/transactions`) — keep in sync with the watcher.
    They previously reached every dependency-less GET, i.e. the whole org
    dataset, from a credential stored in plaintext on a member's gaming PC.
  - **Off-boarding**: `POST /api/admin/members/{id}/access` → `access_revoked()`
    checked in `session_user`/`current_user`/`token_user`. Guild membership is
    only verified at OAuth login, so this is the ONLY thing that ejects a
    removed member before their 8h cookie expires; it also deletes their
    (never-expiring) watcher tokens.
  - **Coordinates are bounded + finite** (`PositionIn`, `_COORD_MAX`). A NaN
    persisted a custom POI that could never be serialized again, permanently
    500ing `/api/custom_pois` org-wide. `validation_error` also strips the
    rejected value from 422 bodies — echoing a NaN back re-raised it.
  - **Org price observations are banded** (`_ORG_PRICE_MAX_RATIO`, judged
    against the pre-overlay UEX snapshot `_uex_price_base`, never the running
    value) and purgeable (`POST /api/admin/prices/clear`). The confirms gate
    validates the learned guid/shop mappings, never the price itself.
  - **Rate limiting**: `rate_limit(bucket, uid)` + `_RATE_LIMITS` — per MEMBER
    (everything limited is authenticated), buckets `solve` (the three planners),
    `watcher` (position + txn ingest), `token` (`/api/tokens`,
    `/download/watcher`). Limits sit far above real use; tests clear
    `app._rate_hits`.
  - **Frontend escaping**: `esc()` covers text AND quoted-attribute context.
    The trap is `.join()` inside a template literal — use `.map(esc).join(...)`.
- **Design**: follow `DESIGN.md` (tokens, components) and `PRODUCT.md` (scope).
- **No build step**: the SPA is served as-is. Don't introduce a bundler.
